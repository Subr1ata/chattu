import { Menu } from "@mui/material";
import PropTypes from "prop-types";

const FileMenu = ({ anchorE1 }) => {
  return (
    <Menu anchorE1={anchorE1}>
      <div
        style={{
          width: "10rem",
        }}
      >
        sadadsaada, sadadsaada, sadadsaada, sadadsaada, sadadsaada, sadadsaada,
        sadadsaada
      </div>
    </Menu>
  );
};

FileMenu.propTypes = {
  anchorE1: PropTypes.any,
};

export default FileMenu;
